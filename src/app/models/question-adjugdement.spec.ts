import { QuestionAdjugdement } from './question-adjugdement';

describe('QuestionAdjugdement', () => {
  it('should create an instance', () => {
    expect(new QuestionAdjugdement()).toBeTruthy();
  });
});
